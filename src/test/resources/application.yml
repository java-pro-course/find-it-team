spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL
    username: postgres
    password: password
    driver-class-name: org.h2.Driver
  liquibase:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false